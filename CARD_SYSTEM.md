# CARD_SYSTEM.md
> 카드 **데이터 구조 + 적용 로직(모디파이어)** 설계서.
> "강화·버프·NPC기술을 어떻게 실제 계산에 반영하나"의 답.
> (카드 흐름·풀·시작덱은 CARDS.md / 전투 규칙은 CORE_COMBAT.md)
> ⚠️ 방향 참조 문서 — 구현하며 더 나은 구조 있으면 변경 가능. 단 각 결정의 **[의도]**는 보존.

---

## 0. 한 줄 요약

```
카드/버프/강화를 "데이터"로 두고, 전투 계산 시 [기본 → 카드강화 → 플레이어버프] 순으로
합쳐서 최종값을 낸다. 이 합치는 과정 = '적용 로직(파이프라인)'.
```

---

## 1. 왜 이 문서가 필요한가 (문제 정의)

지금 프로토타입은 카드 8장이 **코드에 하드코딩**(`CardData.BuildPrototypeDeck`).
→ "마력탄을 강화해서 18→24뎀", "이번 런은 모든 공격 +20%", "NPC가 준 패시브" 같은 걸
   **끼워넣을 자리가 없음.** 값이 고정이라 수정이 불가능.

그래서 필요한 게 **적용 계층**:
- 카드의 "기본값"과 "수정값(강화·버프)"을 분리
- 전투에서 쓸 때 그때그때 합쳐서 "최종값"을 계산

> [의도] 강화/버프/유품/각인/NPC효과가 **전부 같은 통로(모디파이어)**로 흐르게 만들면,
> 콘텐츠를 추가할 때마다 전투 코드를 안 고쳐도 된다 (확장성의 핵심).

---

## 2. 3계층 데이터 모델 (핵심 결정)

### 2-1. CardDefinition — 원본 템플릿 (불변)
```
한 종류의 카드가 "원래" 어떤 카드인지. 게임 전체에서 1개만 존재(공유).
예: 마력탄 = { 공격, 코스트1, 시전1초, 광역, 18데미지 }
```
- 절대 런타임에 안 바뀜 (강화해도 Definition은 그대로).

### 2-2. CardInstance — 내 덱의 그 카드 한 장 (가변)
```
Definition을 가리키면서 + "이 한 장만의 수정 상태"를 가짐.
예: 내 덱의 마력탄 #3 = { def: 마력탄, 강화레벨: 1, 추가데미지: +6 }
```
- **★ 강화 "유지"의 정체** = 강화는 CardInstance에 기록 → 런 동안 그 카드에 영구히 따라다님.
- 덱/손패/무덤은 전부 **CardInstance의 리스트** (지금처럼 CardData 공유 ❌).
- [의도] 같은 마력탄이라도 "강화한 1장"과 "안 한 1장"이 구별돼야 함 → 인스턴스 분리 필수.

### 2-3. PlayerState — 런 전체 상태 (가변)
```
이번 런에서 캐릭터에 걸린 모든 버프·스탯. 예:
  damageMult: 1.2   (모든 공격 +20%)
  drawBonus: +1     (사이클당 드로우 +1)
  costBonus: +1     (코스트 상한 +1)
  maxHpBonus: +20
  barrierMult: 1.25 (베리어 효과 +25%)
  grantedAbilities: [NPC가 준 기술들]
```
- [의도] 캐릭터 단위 버프는 여기 한 곳에. 출처(강화/NPC/유품)는 달라도 결과는 여기로 모임.

---

## 3. 적용 파이프라인 (= 적용 로직)

전투에서 카드를 실제로 쓸 때, 값을 **그때 계산**한다:

```
최종 데미지 = (Definition.기본데미지 + Instance.추가데미지) × PlayerState.damageMult
최종 드로우 = Definition.드로우 + PlayerState.drawBonus
최종 흡수   = (Definition.흡수 + Instance.추가흡수) × PlayerState.barrierMult
...
```

순서 = **[기본값] → [카드 강화(Instance)] → [플레이어 버프(PlayerState)] → 최종**

> [의도] "더하기 먼저, 곱하기 나중" 같은 합성 규칙을 한 곳(파이프라인 함수)에 두면
> 모든 효과가 일관되게 계산됨. 카드별로 제각각 계산하면 버그·불균형의 온상.

구현 형태(예시): `int EffectiveDamage(CardInstance c, PlayerState ps)` 같은 헬퍼 함수로 통일.
전투 코드(ResolveEntry 등)는 **절대 Definition 값을 직접 안 읽고** 이 헬퍼만 호출.

---

## 3.5 버프의 '지속 범위(scope)' — 모디파이어 시스템 (구현됨)

버프는 고정값이 아니라 **Modifier 단위**로 PlayerState에 쌓인다. 각 Modifier는 **언제 사라지는가**가 다름:

```
Modifier { 무엇을(ModStat) / 얼마나(value) / 범위(scope) / 횟수(charges) }

scope:
  Run        런 전체 (유품·영구강화)
  Combat     이 전투 동안     ← ★ 파워 카드 (예: "집중 = 전투 동안 데미지 +50%")
  Cycle      이번 사이클만
  NextCard   다음 카드 1장    ← ★ "다음 카드 강화" (charges=1, 쓰면 소멸)
  NextAttack 다음 공격 1회
```

소멸 규칙:
- 전투 시작 → Combat 범위 초기화 / 사이클 시작 → Cycle 범위 제거
- 카드 사용 시 → NextCard·NextAttack은 charges 차감 후 소멸(ConsumeOnPlay)

> [의도] 파워카드(지속)와 "다음 카드 강화"(1회용)의 차이 = **범위 + 횟수**뿐.
>   둘 다 같은 Modifier로 표현 → 새 버프 카드는 데이터만 추가하면 됨(전투 코드 불변).

## 3.6 강화 경로(종류) — 한 카드도 여러 강화 버전

> 강화 = "수치 +"만이 아님. 카드마다 어떤 강화가 가능한지 다름.

| 강화 종류 | 예 | 데이터(CardInstance) |
|---|---|---|
| 수치 강화 | 마력탄 18→24, 베리어 30→40 | bonusDamage / bonusAbsorb (있음) |
| 코스트 감소 | 메테오 3→2코 | bonusCost (추가 필요) |
| 시전 단축 | 체인 1.5→1.0초 | bonusCastTime (추가 필요) |
| 추가 효과/키워드 | 마력탄에 슬로우 부여 / 광역화 / 관통 / 소진 제거 | 효과 플래그·Modifier (추가 필요) |
| 분기 선택(2택) | 강화 시 "데미지+" vs "코스트-" 택1 (슬더슬식) | upgradeChoice (TBD) |

- 현재 구현: `bonusDamage / bonusAbsorb / upgradeLevel`. → 코스트·시전·키워드 필드로 확장 예정.
- 강화 페이지(card_upgrade_v1)는 **그 카드가 가능한 강화만** 미리보기로 제시.
- [의도] 강화 경로가 다양해야 "같은 카드도 빌드마다 다르게 큰다" = 깊이.

## 3.6.1 강화 심화 — 등급 / 수치 규칙 / 전승 통제 (시뮬레이션 기반)

### (a) 강화 등급 3단계 — 소스로 게이팅
```
일반 강화  = 회복방(제단). 아쉬운 수치 강화 (예: 데미지 +10%). 흔함.
희귀 강화  = NPC·제단. 나쁘지 않은 강화 + 특수효과 몇 가지 (코스트↓·시전↓·드로우 등).
전설 강화  = 특수 NPC·특수조건·숨겨진 방. 강한 효과/키워드(슬로우·관통·광역화·+100%).
            의도적 밸런스 파괴, 획득 난이도로 자연 제한.
```

### (b) 데미지 계산 — 카드 단위 가산, 버프층만 ×(1+합), 전부 내림
```
카드 데미지 = base + Σ(강화)            ← 전부 더하기 (선형, 무한 가능 = "영수증 카드")
   · 고정강화: +5
   · %강화:   + 내림(base × 0.1)  → 100→110→120…  (base 기준 가산, 곱연산 아님)
최종 데미지 = 내림( 카드데미지 × (1 + Σ버프%) )
   · 버프(축복/파워/영구)는 여기서만. "총뎀 + 총뎀×버프%" = ×(1+합).
   · 버프끼리 '합쳐서 1번' 적용 → 곱연산 폭주 아님.
모든 결과 소수점 내림(floor).
```
→ 카드 단위가 선형이라 무한 강화해도 통제됨. (구현: CardMath.Damage, FloorToInt)

### (c) 전승(헤리티지) 통제 = '총 강화 횟수' (점수제 폐기)
```
[의도] 전승으로 다음 캐릭이 쉬워짐(스노볼) → 강화 많이 한 카드는 전승 제한.

규칙: 카드 누적 강화 '횟수' ≤ 임계(예: 5 또는 10) → 전승 가능.
      초과 시 → "이 카드는 전승할 수 없습니다" 표시. (그 런에서는 계속 써도 됨)
등급 무관, 횟수만 셈 (전설 5번 = 5회 = 노상관).

결과:
  무한층 빌드  → 강화 막 함 (전승 안 할 거라 신경 끔)
  전승용 달리기 → 강화 절제(횟수 유지) = 전략적 자제

※ 점수제(일1/전5) 폐기 이유: "전설3 vs 일반15" 역전 등 빡빡함 발생.
※ "전설 5강 지존카드" 같은 극단 케이스는 필요 시 별도 규칙 (TBD).
```
구현: CardInstance.upgradeLevel(이미 있음) 사용 → `CanInherit => upgradeLevel <= 임계`.
임계·수치는 전부 튜닝값(임시).

## 3.7 두 분류 축 — 등급(레어도) × 역할(기능)

> [의도] 헷갈리면 밸런스 붕괴. 두 축은 독립이다.
> · **등급** = 얼마나 강하고 귀한가 (획득 빈도·파워 예산)
> · **역할** = 무엇을 하는가 (딜/방어/유틸/파워·셋업)

### 등급 (4단계)

| 등급 | 성격 | 보상 등장 가중치(임시) | 시각(테두리) |
|---|---|---|---|
| 일반 Common | 기본기·견실, 덱을 채움 | 60% | 회색 |
| 희귀 Rare | 조건부·셋업·시너지 | 28% | 파랑 |
| 영웅 Epic | 강력기·핵심 콤보 부품 | 9% | 보라 |
| 전설 Legendary | 판 바꾸는 빌드의 축(파워카드 등) | 3% | 주황 |

- 차이 = ① 보상/상점 빈도 ② 파워 예산 ③ (추후) 가격. 시작 덱은 대부분 일반.
- 코드: `CardDefinition.rarity`, `RarityColor()/RarityName()`. 전투 로직엔 영향 없음(획득 단계용).

### 역할 (기능 분류 — 데미지/역할 기준, 추후 태그화)

```
딜      직접 데미지 (마력탄·체인·메테오)
방어    흡수·회피 보조 (베리어·마나실드)
유틸    드로우·자원·슬로우 셋업 (신속한 사고·냉기폭발)
파워    지속 버프(소진) (집중)
셋업    다음 카드/콤보 강화 (예리함)
```
> 역할은 카드 풀 설계·밸런싱·시너지(아키타입)용 분류. 추후 `CardDefinition`에 태그로 추가 가능.

## 4. 세 가지 예시가 어떻게 작동하나 (너의 질문 매핑)

### ① 캐릭터 데미지 상승 버프
```
PlayerState.damageMult = 1.2 로 설정 (NPC/유품/이벤트가 설정)
→ 이후 모든 공격 카드 데미지가 파이프라인에서 ×1.2
→ 카드 데이터는 안 건드림. 버프 해제 = 다시 1.0
```

### ② 특정 카드 강화 유지
```
모닥불에서 "마력탄 강화" 선택
→ 그 CardInstance.강화레벨 +1, 추가데미지 +6 기록
→ 런 내내 그 카드는 24뎀으로 동작 (인스턴스에 박혀 있으므로 유지)
→ 다른 마력탄(안 강화한 것)은 그대로 18뎀
```

### ③ 특정 NPC 기술 사용
```
두 가지 구현 방식 (TBD):
 (a) 카드로 부여 = NPC가 새 CardInstance를 덱에 추가 (가장 단순, 권장 시작점)
 (b) 패시브로 부여 = PlayerState.grantedAbilities에 등록 → 조건부 자동 발동
→ 우선 (a)로 시작(카드 = 모든 능력의 공통 그릇), 필요 시 (b) 확장
```

---

## 5. 버프/조작의 출처 (어디서 적용되나)

적용 로직은 같고, **트리거 장소만 다름**:

| 출처 | 무엇을 바꾸나 | 필요한 UI |
|---|---|---|
| 전투 보상 | CardInstance 덱에 추가 | 카드 선택창 |
| 상점 | 카드 추가/제거, 유품 구매 | 상점 UI |
| 모닥불 | CardInstance 강화, 회복 | 선택창 |
| 이벤트(NPC) | PlayerState 버프, 카드 부여/변형 | **대화·선택창** ← 사용자 지적 |
| 유품/각인 | PlayerState 지속 수정 | (자동) |

> [의도] 이 모든 게 "CardInstance 조작" 또는 "PlayerState 조작"으로 환원됨.
> = 대화 UI 등 조우 시스템은 **이 두 데이터를 바꾸는 입력 장치**일 뿐.
> → 그래서 **데이터 구조(2장) + 파이프라인(3장)이 먼저**, 대화 UI는 그 위에.

---

## 6. 구현 영향 (현재 → 목표)

```
현재: CardData(공유 객체) 8장 하드코딩, ResolveEntry가 c.damage 직접 사용
목표:
  1) CardData → CardDefinition 으로 (불변 템플릿)
  2) CardInstance 신설 (def 참조 + 강화/수정 필드), 덱·손패·무덤·큐를 Instance로
  3) PlayerState 신설 (런 버프)
  4) EffectiveXxx() 파이프라인 헬퍼 신설, 전투 코드가 이것만 호출
  5) (이후) 카드 풀 확장 → 보상/상점/모닥불/이벤트 → 대화 UI
```
- 1~4는 **전투만으로 검증 가능**(대화 UI 불필요) → 먼저 함.
- 5의 조작 UI(대화 포함)는 1~4가 있어야 의미.

---

## 7. TBD

1. 강화 방식 — 단일 경로(레벨업) vs 분기(슬더슬 +업글 2택) vs 키워드 부여
2. NPC 기술 부여 = 카드(a) vs 패시브(b) 비중
3. 모디파이어 합성 규칙 — 가산/승산 순서, 상한
4. 유품/각인을 PlayerState에 어떻게 표현할지 (효과 리스트 vs 개별 필드)
5. 카드 변형(transform)이 Instance 교체인지 def 교체인지
6. 저장: 런 중단 후 복귀 시 Instance/PlayerState 직렬화 (메타 단계)
